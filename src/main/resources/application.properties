spring.application.name=DALYDA_BACKEND_STOCKMANAGER

# database configuration
spring.datasource.url=${SPRING_DATASOURCE_URL:jdbc:postgresql://localhost:5432/DALYDA}
spring.datasource.username=${SPRING_DATASOURCE_USERNAME:postgres}
spring.datasource.password=${SPRING_DATASOURCE_PASSWORD:postgres}
spring.datasource.driver-class-name=org.postgresql.Driver
spring.jpa.hibernate.ddl-auto=${SPRING_JPA_HIBERNATE_DDL_AUTO:update}
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect

# Email Sending Configurations
spring.mail.host=${SPRING_MAIL_HOST:smtp.gmail.com}
spring.mail.port=${SPRING_MAIL_PORT:587}
spring.mail.username=${SPRING_MAIL_USERNAME:hitimanadav47@gmail.com}
spring.mail.password=${SPRING_MAIL_PASSWORD:yfpd vtmw emrc talm}
spring.mail.properties.mail.smtp.auth=${SPRING_MAIL_PROPERTIES_MAIL_SMTP_AUTH:true}
spring.mail.properties.mail.smtp.starttls.enable=${SPRING_MAIL_PROPERTIES_MAIL_SMTP_STARTTLS_ENABLE:true}
spring.mail.from=${SPRING_MAIL_FROM:no-reply@dalyda.com}

# JWT Secret (must be Base64 encoded, minimum 32 bytes)
jwt.secret=${JWT_SECRET:Y7FkT0KxcpHb9y5A4UO3fZzw6sAo+Pld+ArwxwQZscU=}